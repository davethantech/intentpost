let selected=null;
const $=id=>document.getElementById(id);
async function api(path,options={}){const r=await fetch(path,{credentials:'include',...options,headers:{'content-type':'application/json',...(options.headers||{})}});if(r.status===401){showLogin();throw new Error('authentication_required')}const text=await r.text();let data={};try{data=JSON.parse(text)}catch{}if(!r.ok)throw new Error(data.error||`Request failed (${r.status})`);return data}
function showLogin(){$('appView').hidden=true;$('loginView').hidden=false}
function money(v){return new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(Number(v||0))}
async function boot(){try{const me=await api('/api/auth/me');$('loginView').hidden=true;$('appView').hidden=false;$('identity').textContent=`${me.user.name} · ${me.user.role}`;$('roleBadge').textContent=me.user.role.toUpperCase();$('workspaceInfo').textContent=`Workspace: ${me.user.organization_id}`;await loadOverview();$('status').textContent='● secure session'}catch{showLogin()}}
async function loadOverview(){const x=await api('/api/dashboard/overview');$('health').textContent='ONLINE';$('mAccounts').textContent=x.metrics.accounts;$('mSignals').textContent=x.metrics.signals;$('mCampaigns').textContent=x.metrics.campaigns;$('mTouchpoints').textContent=x.metrics.touchpoints;$('mSpend').textContent=money(x.metrics.touchpoint_spend);$('accountList').innerHTML=x.accounts.map(a=>`<button class="account-row ${selected?.id===a.id?'selected':''}" data-id="${a.id}"><span><b>${escapeHtml(a.name)}</b><small>${escapeHtml(a.industry||'')} · ACV ${money(a.acv)}</small></span><span class="scores"><strong>${Math.round(a.intent_score)}</strong><small>intent</small><strong>${Math.round(a.physical_intervention_score)}</strong><small>physical</small></span></button>`).join('');document.querySelectorAll('.account-row').forEach(b=>b.onclick=()=>selectAccount(b.dataset.id));if(!selected&&x.accounts[0])selectAccount(x.accounts[0].id)}
async function selectAccount(id){selected=await api('/api/dashboard/account/'+encodeURIComponent(id));$('evaluate').disabled=false;$('selectedAccount').innerHTML=`<div class="account-title"><div><h3>${escapeHtml(selected.account.name)}</h3><p>${escapeHtml(selected.account.domain||'')} · ${escapeHtml(selected.account.industry||'')}</p></div><div class="score-box"><b>${Math.round(selected.account.intent_score)}</b><span>intent</span><b>${Math.round(selected.account.physical_intervention_score)}</b><span>physical</span></div></div><div class="contact"><b>${escapeHtml(selected.contacts[0]?.name||'No contact')}</b><span>${escapeHtml(selected.contacts[0]?.title||'')}</span><span>Address: ${escapeHtml(selected.contacts[0]?.address_status||'unknown')}</span></div>`;renderSignals();renderGraph();document.querySelectorAll('.account-row').forEach(b=>b.classList.toggle('selected',b.dataset.id===id))}
function renderSignals(){$('signalTimeline').innerHTML=selected.signals.length?selected.signals.map(s=>`<div class="signal"><span class="signal-score">${Math.round(s.strength)}</span><div><b>${escapeHtml(s.signal_name)}</b><small>${escapeHtml(s.signal_type)} · ${new Date(s.occurred_at).toLocaleString()}</small></div></div>`).join(''):'<p class="muted">No signals recorded.</p>'}
function renderGraph(){const a=selected.account,nodes=[{label:a.name,type:'ACCOUNT'},...selected.contacts.slice(0,3).map(c=>({label:c.name,type:'CONTACT'})),...selected.signals.slice(0,5).map(s=>({label:s.signal_name,type:'SIGNAL'}))];$('graph').innerHTML=nodes.map(n=>`<div class="node"><b>${escapeHtml(n.label).slice(0,28)}</b><small>${n.type}</small></div>`).join('<div class="edge">→</div>')}
async function evaluate(){if(!selected)return;const btn=$('evaluate');btn.disabled=true;btn.textContent='Evaluating…';$('decision').innerHTML='<div class="loading">Running policy + decision engine…</div>';try{const x=await api('/api/ai/evaluate',{method:'POST',body:JSON.stringify({accountId:selected.account.id,contactId:selected.contacts[0]?.id,campaignId:'camp_demo',cost:12})});$('decision').innerHTML=`<div class="decision-card"><div class="decision-main"><span class="decision-pill">${escapeHtml(x.decision||'ERROR')}</span><b>${escapeHtml(x.experience||'NONE')}</b><span>${money(x.recommended_spend||0)} recommended spend</span></div><p>${escapeHtml(x.reasoning_summary||x.error||'No reasoning returned.')}</p><div class="decision-meta">Intent ${Math.round(x.intent_score||0)} · Physical ${Math.round(x.physical_intervention_score||0)} · Confidence ${Math.round((x.confidence||0)*100)}%</div></div>`}catch(e){$('decision').innerHTML=`<div class="error">${escapeHtml(e.message)}</div>`}finally{btn.disabled=false;btn.textContent='Evaluate selected'}}
function escapeHtml(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
$('loginForm').addEventListener('submit',async e=>{e.preventDefault();$('loginError').textContent='';try{await api('/api/auth/login',{method:'POST',body:JSON.stringify({email:$('email').value,password:$('password').value})});await boot()}catch(err){$('loginError').textContent='Invalid email or password.'}});
$('logout').onclick=async()=>{await api('/api/auth/logout',{method:'POST'}).catch(()=>{});selected=null;showLogin();$('password').value=''};
$('evaluate').onclick=evaluate;boot();
